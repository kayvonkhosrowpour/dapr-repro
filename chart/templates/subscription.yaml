apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: dapr-repro-incoming
spec:
  pubsubname: {{ .Values.dapr.pubsubName }}
  topic: {{ .Values.dapr.incomingTopic | quote }}
  routes:
    default: /events/incoming
  scopes:
    - {{ .Values.dapr.appId }}
